[
  {
    "name": "predict:match callback shows match not found for invalid id",
    "steps": [
      {
        "send": { "callback": "predict:match:nonexistent" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Couldn't find that match — it may have been removed."
            }
          }
        ]
      }
    ]
  }
]
