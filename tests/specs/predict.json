[
  {
    "name": "/predict shows empty state when no matches",
    "steps": [
      {
        "send": { "text": "/predict" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No matches to predict yet — check back soon!"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "predict menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "predict:start" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No matches to predict yet — check back soon!"
            }
          }
        ]
      }
    ]
  }
]
