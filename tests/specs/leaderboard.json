[
  {
    "name": "/leaderboard shows empty state when no predictions",
    "steps": [
      {
        "send": { "text": "/leaderboard" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No predictions yet — be the first to predict a match!"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "leaderboard menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "leaderboard:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No predictions yet — be the first to predict a match!"
            }
          }
        ]
      }
    ]
  }
]
