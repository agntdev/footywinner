[
  {
    "name": "/matches shows empty state when no matches",
    "steps": [
      {
        "send": { "text": "/matches" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No upcoming matches right now — check back soon!"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "matches menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "matches:list" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No upcoming matches right now — check back soon!"
            }
          }
        ]
      }
    ]
  }
]
