[
  {
    "name": "/myguesses shows empty state when no predictions",
    "steps": [
      {
        "send": { "text": "/myguesses" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "You haven't made any predictions yet — tap Predict to get started!"
            }
          }
        ]
      }
    ]
  },
  {
    "name": "myguesses menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "myguesses:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "You haven't made any predictions yet — tap Predict to get started!"
            }
          }
        ]
      }
    ]
  }
]
